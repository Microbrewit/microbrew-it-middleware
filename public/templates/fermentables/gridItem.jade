.grid-item.search-grid-result
	a.content(href='fermentables/#{item.fermentableId}')
		.top-circle
			span(class='minibubble' style="display:inline-block;width:50px;height:50px;padding:0;margin:0;border-radius:50%;border:1px solid rgba(0,0,0,0.2);background:rgba(#{convert(item.lovibond, 'lovibond', 'rgb')},1);")
			img(src='images/fermentable-large.png', width='50', height='50', alt='Malt')
		h3 #{item.name}
		.type Fermentable
		.meta
			.meta-table
				.meta-cell #{item.lovibond}°L
				.meta-cell #{item.ppg} PPG