article.login
	section.login-form
		.inner-wrapper
			form( action="/login", method="post")
				label(for="username") Username
				input(type="text" id="password" name="username" placeholder="johnfredrik" value = "johnfredrik" required)
				label(for="password") Password
				input(type="password" id="password" name="password" placeholder="password" value="SuperPass" required)
				label(for="remember") keep loggen in
				input(type="checkbox" id="remember")
				button(class="submit" type="submit") Login